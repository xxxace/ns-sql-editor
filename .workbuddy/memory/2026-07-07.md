# 2026-07-07 工作日志

## 连接功能「导出复制 / 导入粘贴」方案规划

- 需求：在「连接管理」(`LoginDialog`) 增加 复制（到剪贴板）+ 粘贴导入 能力。
- 已做能力盘点：复用 `auth.exportAccounts`（文件下载，不动）、`db.saveAccount/getAllAccounts`、`StoredAccount`。
- 用户确认 4 项决策：①单条+批量都支持 ②导出含明文密码 ③粘贴用弹窗文本框手动粘贴（兼容 HTTP，不依赖 clipboard.readText）④导入仅入列不连接、重名跳过。
- 产出方案文档：`连接功能-导出复制导入粘贴-开发方案.md`（v0.1，待实现确认）。
- 设计要点：新增 `utils/clipboard.ts`(带 fallback 的 copy) + `ConnectionTransferDialog.vue`(export/import 双模式)；auth store 新增 `serializeConnections/parseConnections/importConnections`。改动面小、风险低。
- 待用户确认方案后进入实现。

## 连接功能「导出复制 / 导入粘贴」已实现

- 用户关键反馈：**导入/导出纯逻辑必须独立成模块（目录），由消费者调用，不可全塞进 `auth` store 耦合**。据此调整为分层架构：
  - `src/utils/connectionTransfer.ts`（纯模块：serialize/parse/resolveImportItems，无 Pinia/UI 依赖）
  - `src/utils/clipboard.ts`（copyToClipboard，带 HTTP fallback）
  - `src/stores/auth.ts`：仅 1 个编排方法 `importConnections`，逻辑委托模块
  - `src/components/ConnectionTransferDialog.vue`（export/import 双模式弹窗）
  - `src/components/LoginDialog.vue`：每条连接「复制」+ 页脚「复制全部 / 粘贴导入」+ 挂载弹窗
- 图标：复制=DocumentCopy，导入=Upload（Paste 在本版 @element-plus/icons-vue 不存在）。
- 验证：`npm run build`（vue-tsc 类型检查 + vite build）通过，仅第三方 @vueuse 注释告警。
- 方案文档已同步为最终分层架构（v0.2）。

## 20:30 追加：创建 AGENTS.md
- 在根目录创建 `AGENTS.md`，沉淀用户定义的 **CDD（Capability-Driven Development）角色与任务执行规则**：T0 紧急/阻断、常规开发（强制能力盘点与复用）、轻量级三类分级与对应策略。
- 文件附加：项目概览（技术栈/脚本/目录）、开发约定（解耦优先、连接功能模块位置）、给后续 agent 的提示，使其真正"助力开发"。
- 用户意图：后续开发（含其他 agent / 后续会话）默认遵循该规则。

## 20:34 追加：审查 AGENTS.md
- 逐条对照代码核验 AGENTS.md 事实性断言：函数名/签名/行为、`auth.importConnections` 编排、`db.ts` 导出、按钮与图标、明文密码提示、导入行为、build 脚本——均一致，无硬性错误。
- 修正 2 处轻微误导：① §3.1「解耦优先」改为"针对新增代码"，明确 auth.ts 仍含登录/会话逻辑不强行迁移；② §4 联动逻辑补指向 `src/stores/linkage.ts`/`src/api/linkage.ts`/`src/components/linkage/`（已确认联动功能已实现）。
- 结论：原文档无错误，仅修两处措辞避免走错方向。

## 20:37 追加：AGENTS.md 去冗余精简
- 用户要求审查"是否有不必要/不该出现的内容"。分析判定 3 处冗余并精简：
  1. 技术栈删掉子依赖罗列 `（`@nameson/sqlutils`、`sql-formatter` 等）`（权威源是 package.json）。
  2. 删除「初始化指令」应答式段落（与 AGENTS.md 定位不符的语气残留）。
  3. §三.3 连接功能去重：只保留行为约定（明文密码/安全提示/不自动连接/重名跳过），模块细节回指 §二，消除双真相源。
- 注意：第一次整行替换编辑报"成功"但未生效（疑似不可见字符差异），改用最小锚点片段删除才成功。最终通读确认文档干净、无重复。
