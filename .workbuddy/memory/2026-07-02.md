# 2026-07-02

## 登录账户联动功能 — 全部实施完成（v0.4 方案）

### 实施内容（3 Phase，7 个任务，构建通过 ✓）

**Phase 1: 基础设施**
- `src/api/statement.ts` — getMaxTabseq / buildInsertModel / buildUpdateModel / createStatement（共享底层）
- `src/api/nameson.ts` — 新增 searchDataWithAuth() + saveDataWithAuth()（自定义认证参数，目标会话查询）
- `src/stores/auth.ts` — 新增 silentLogin(account)（静默登录，不切换 authStore 状态）

**Phase 2: Feature A — 新增语句**
- `src/components/NewStatementDialog.vue` — DSNAME 输入弹窗，创建占位语句，创建后自动切换+解锁编辑
- `src/components/StatementList.vue` — 面板头新增"+"按钮（Plus 图标），刷新按钮左边
- 创建后自动 refresh 清单 + selectStatement + isLocked=false

**Phase 3: Feature B — 联动对比**
- `src/stores/linkage.ts` — 联动状态管理（目标会话、对比结果、同步队列、diffTabseq 等）
- `src/api/linkage.ts` — fetchTargetStatements / syncStatement / updateFromExternal / locateTargetObjectId
- `src/components/SessionExpiredDialog.vue` — 通用会话过期弹窗（主/目标通用）
- `src/components/linkage/PageSelector.vue` — 主会话页面下拉（当前锁定）
- `src/components/linkage/SessionSelector.vue` — 目标会话下拉选择器
- `src/components/linkage/MainStatementList.vue` — 左列主会话清单（红/灰/黄状态标记）
- `src/components/linkage/TargetStatementList.vue` — 右列目标会话清单（含同步/更新按钮）
- `src/components/linkage/DiffPanelView.vue` — 联动专用 Diff（props 接收，不依赖 editorStore）
- `src/components/linkage/LinkageCore.vue` — 核心编排（对比逻辑、会话过期处理、批量同步）
- `src/components/linkage/LinkageDialog.vue` — 全屏弹窗容器
- `src/components/SqlToolbar.vue` — 新增"联动对比"按钮（Link 图标）
- `src/views/EditorView.vue` — 集成 LinkageDialog + linkageStore

### 对比逻辑
- DSNAME 空 → 红色 + ⚠
- 仅主会话有 → 灰色（左列）
- 仅目标会话有 → 灰色 + 同步按钮（右列），支持批量勾选同步
- 内容一致 → 正常显示
- 内容差异 → 黄色标记 + 可展开 Diff + 「从外部更新」按钮
- UPDATE 覆盖字段: DBQUERY, SORTBYCONTENT, REF1

### 待后续验证
- 目标环境 PRJOBJECT 表 ENAME→OBJECTID 定位逻辑需连接实际服务端测试
- 会话过期 HTTP 错误码检测逻辑需根据实际 API 响应调整

## 登录账户联动功能 — 开发方案编写

- 基于 `登录账户和其它账户联动功能需求.txt` 和当前 ns-sql-editor 项目代码探索，输出了完整开发方案文档
- 方案文档: `登录账户和其它账户联动功能-开发方案.md`

### 关键决策（方案建议）

1. **架构选型**: 推荐「混合模式」— 独立 linkageStore + 嵌入主界面的 LinkagePanel，而非独立模块或全嵌入
2. **对比匹配**: 以 DSNAME 为主匹配键，DBQUERY hash 为辅助
3. **同步方向**: 仅单向（目标→主会话）
4. **并发方案**: 乐观锁+重试（前端），理想情况后端提供原子化接口
5. **UI 形态**: 侧边抽屉（复用 DraftDrawer 模式）

### 用户第一轮反馈（已全部确认）

1. 只有一个主登录，其它自己切换作为目标会话
2. 两边语句都列出，主会话缺失的灰色显示
3. 主界面不涉及会话切换，只绑定主会话；目标会话选择在弹窗内
4. DSNAME 同一页面下唯一，不需要 hash 辅助
5. 仅单向：目标→主会话
6. 差异语句可"从外部更新"(UPDATE)，覆盖主会话 DBQUERY/SORTBYCONTENT
7. DSSTMSEQ=语句序号, DSSTMTY=语句类型(=TABSEQ)
8. 保存用主会话登录信息，源数据字段复制
9. 不考虑并发，唯一约束兜底
10. DSNAME 为空标红色异常
11. 弹窗+全屏，组件可拆分(组件+弹窗/抽屉/嵌入)
12. 无批量上限
13. 未明确字段留空，同步复制源数据

### 方案文档更新至 v0.2

- 架构改为「组件+弹窗」：LinkageCore(核心组件) + LinkageDialog(弹窗容器)
- 主界面仅 SqlToolbar 加一个按钮，零侵入
- 新增 UPDATE 流程（差异语句"从外部更新"）
- 并发方案简化为不做处理

### 用户第二轮反馈（已全部确认）

1. 新增语句按钮放在 StatementList 刷新按钮左边
2. 主界面不新增会话选择功能，页面清单和语句清单只绑定主会话，切换只能通过现有"切换连接"
3. 新增语句弹窗：用户填写基本信息（DSNAME），不需要填 DBQUERY，创建成功后切换到该语句由用户编写 → 创建占位语句

### 方案文档更新至 v0.3

- 新增 Feature A: 新增语句（占位创建）— 独立弹窗，共享 getMaxTabseq + buildInsertModel
- Feature B: 联动对比与同步 — 弹窗+全屏，组件可拆分
- 两套功能共享 api/statement.ts 底层 INSERT 机制
- StatementList 面板头改动: `[+] [↻刷新]`
- 新增 Q8-Q9（新增语句弹窗字段范围、创建后是否自动解锁编辑器）

### 用户第三轮反馈（Q1-Q9 全部确认）

1. Q1: UPDATE 覆盖 DBQUERY + SORTBYCONTENT + REF1（语句说明）
2. Q2: **左右双列对照**（非统一混排列表）。左=主会话清单，右=目标会话清单。DSNAME 空标红，不在对方列表标灰。上方双下拉（主页面+目标会话）
3. Q3: 锁定当前页面，主会话切换只能通过"切换连接"
4. Q4: 刷新前如有未保存变更→弹窗提示
5. Q5: DiffPanel 职责单一，仅对比展示，更新操作由外层 SyncActions 管理
6. Q6: 目标会话连接过期→**弹窗让用户点击刷新会话**（不是自动静默刷新，也不是报错让用户手动切）。主会话调现有 reconnect()，目标会话需新增 silentLogin()（不切换 authStore 状态）
   - 需新增: searchDataWithAuth() (api/nameson.ts) + silentLogin() (stores/auth.ts) + SessionExpiredDialog.vue
7. Q7: 行级标红+⚠异常图标，不阻止其他操作
8. Q8: 仅 DSNAME 必填，其余留空
9. Q9: 创建后自动解锁编辑器

### 方案文档更新至 v0.4

- 清单对比从统一列表改为左右双列表对照
- UPDATE 增加 REF1 字段
- 新增刷新前未保存变更检查流程
- DiffPanel 简化（职责单一，仅对比展示）
- Q6 设为技术调查项（目标会话自动刷新）
- 组件拆分细化：PageSelector / SessionSelector / MainStatementList / TargetStatementList

### 六点修复（15:19-15:35，构建通过 ✓）
1. **切换会话刷新**：MenuTree + StatementList 加 watch(auth.currentName)，切换时 clear+reload
2. **当前会话标记**：LoginDialog 加 is-current class，绿色左边框 + `(当前)` tag
3. **改名**："账户联动对比"→"跨服务对比"，全局 LinkageDialog + SqlToolbar
4. **排除自身**：SessionSelector 过滤 auth.currentName，无其他会话时 disabled + hint
5. **页面可切换**：PageSelector 从 disabled input → el-select；linkageStore 新增 selectedPageObjectId；LinkageCore watch 联动两者
6. **Diff 全屏**：diffTabseq 激活时隐藏双列清单，DiffPanelView flex:1 占满空间；"从外部更新"按钮移至 DiffPanelView header

### 三点问题修复（15:38 后，构建通过 ✓）
1. **状态栏蓝色背景**：`SqlEditor.vue` 中 `.status-bar` 改为 `background: #007acc; color: #fff`（VS Code 蓝）
2. **主会话清单独立加载**：`linkageStore` 新增 `mainStatements` + `loadingMain`；`LinkageCore` 拆分单一 watch 为两个独立 watch（`selectedPageObjectId`→主会话 / `targetSession`→目标会话），分别加载各自清单；`MainStatementList` 从 `mainStatements` 渲染（选页面即有数据），`compareResults` 只叠加状态颜色
3. **目标会话清单独立加载**：`TargetStatementList` 从 `targetStatements` 渲染（选目标会话即有数据），`compareResults` 只叠加状态颜色；`maybeCompare()` 在两边数据都就绪时自动触发对比

### 弹窗初始页面 + 联动重载 + 刷新按钮（15:52，构建通过 ✓）
1. **弹窗自动带入主界面页面**：EditorView.handleOpenLinkage() 先调 linkage.setSelectedPage() 再 openDialog()
2. **页面切换联动目标重载**：提取 reloadTargetStatements() 复用函数；Watch #1 中 targetSession 存在时自动重载目标清单
3. **两个清单刷新按钮**：MainStatementList + TargetStatementList header 各加 Refresh 按钮，disabled 跟随 loading/无页面/无会话

### 弹窗页面下拉框不显示选中（16:00，构建通过 ✓）
1. **根因**：`PageSelector.vue` 的 `pageOptions` 从 `editor.menuNodes` 取根菜单（如"APS"），但 `editor.currentObjectId` 是子菜单页面（如"需求任务单"），`objectId` 不匹配 → `el-select` 显示 placeholder
2. **修复**：`editor.ts` 的 `MenuNode` 接口加 `children`/`label`/`hasChild` 可选字段；`PageSelector` 的 `pageOptions` 改为扁平化 `root.children`（所有子菜单页面）
3. **效果**：打开弹窗时自动带入主界面选中页面，下拉框正确显示页面名称

### 弹窗标题主会话标签（16:20，构建通过 ✓）
1. **需求**：弹窗标题旁显示当前主会话信息，方便用户核对
2. **实现**：`LinkageDialog.vue` 用 `#title` slot 自定义标题区域，左侧"跨服务对比"，右侧 `el-tag` 显示 `currentName (currentUser@serverUrl)`
3. **样式**：tag 使用 `type="info"` + `effect="plain"`，max-width 400px + 文本截断，避免标题区域过宽

