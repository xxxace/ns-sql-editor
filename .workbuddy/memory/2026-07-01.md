# 2026-07-01 — NS SQL Editor 项目启动

## 完成内容
- 接手寇豆码遗留项目，用 `create-vite` 脚手架重建（删除手写配置）
- 安装全部最新依赖（Vue 3.5, Vite 8, Pinia 3, Element Plus 2.14, Monaco 0.55）
- 完成 18 个源文件的编写，覆盖基础设施 → 组件 → 视图 → 入口全链路
- `npm run build` 通过，dev server 正常启动

## 技术决策
- 登录/账户管理用弹窗而非独立路由（用户要求）
- API 层静态导入 authStore 自动注入 pUser/p_sessionID
- Monaco Editor 完整打包（monaco-editor），非 CDN
- Diff 用内联侧边面板（非弹窗）
- 暗色主题：工业实用风 + 终端元素融合
- 工具栏新增「添加连接」「切换连接」按钮

## 去掉了 vue-router
用户指出登录应该用弹窗而非独立 /login 路由，项目退化为单视图应用，移除了 vue-router。
