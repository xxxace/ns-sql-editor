# 连接功能 — 导出（复制）/ 导入（粘贴）开发方案

> **文档状态**: v0.2（已实现，`vue-tsc` 类型检查 + `vite build` 通过，2026-07-07）
> **编写日期**: 2026-07-07
> **对应需求**: 在「连接管理」中增加 复制（到剪贴板）与 粘贴导入 能力
> **相关技术栈**: Vue 3 + Pinia + Element Plus（沿用现有 `LoginDialog` / `auth` store / `utils/db`）

---

## 0. 决策记录（已与用户确认）

| # | 决策点 | 结论 |
|---|--------|------|
| D1 | 复制/粘贴范围 | **单条 + 批量都支持**（每条连接一个「复制」，另提供「复制全部」） |
| D2 | 密码是否包含 | **包含密码**（明文写入导出文本，便于接收方直接连接） |
| D3 | 粘贴交互形态 | **弹窗文本框手动粘贴**（点击按钮弹出文本框，用户 Ctrl+V；不依赖 `navigator.clipboard.readText`，兼容 HTTP 本地环境） |
| D4 | 导入后行为 | **仅加入「已保存连接」列表，不自动连接；名称已存在则跳过该条** |

---

## 1. 现状与复用盘点

### 1.1 现有能力（直接复用，不新造）

| 资产 | 位置 | 复用方式 |
|------|------|---------|
| `auth.exportAccounts()` | `src/stores/auth.ts:148` | 现有「导出清单」（文件下载，不含密码）。保留不动，作为"导出文件"通道；新增"复制"走新序列化逻辑 |
| `saveAccount()` / `getAllAccounts()` | `src/utils/db.ts` | 导入落库直接复用 `saveAccount` |
| `StoredAccount` 类型 | `src/utils/db.ts:11` | 导入构造新账号时复用其结构 |
| `LoginDialog.vue`「已保存连接」页 | `src/components/LoginDialog.vue` | 新增按钮 + 弹窗挂载点 |
| `ElMessage` / `el-dialog` / `el-input` | 已引入 | 提示与弹窗 UI |

### 1.2 缺口（需要新增的部分）

1. **序列化/反序列化工具**：单条与批量的 JSON 互转 + 校验。
2. **剪贴板写入**：因本地多为 HTTP，`navigator.clipboard.writeText` 可能不可用，需要带 fallback 的 `copyToClipboard()`。
3. **导入动作**：解析 → 校验 → 过滤重名 → 批量 `saveAccount`。
4. **UI**：每条连接「复制」按钮 + 页脚「复制全部 / 粘贴导入」 + 一个共享弹窗组件。

---

## 2. 序列化格式

### 2.1 单条连接导出文本

```json
{
  "name": "生产环境-主库",
  "serverUrl": "http://10.0.0.1:9001/xxx.asmx",
  "user": "admin",
  "password": "plaintext-pwd"
}
```

### 2.2 批量导出文本（「复制全部」）

JSON 数组，元素结构同上：

```json
[
  { "name": "生产环境-主库", "serverUrl": "...", "user": "admin", "password": "..." },
  { "name": "测试库", "serverUrl": "...", "user": "tester", "password": "..." }
]
```

### 2.3 解析兼容规则（导入侧）

- 接受 **单个对象** 或 **对象数组** 两种形态（自动 `Array.isArray` 判断）。
- 每条必填：`name`（非空字符串）、`serverUrl`（非空字符串）。
- 可选：`user`、`password`（缺失则置空字符串，仍可加入列表，连接时需补填）。
- **刻意排除** `sessionId` / `lastLoginAt` 等运行时字段（非配置，不应被导入）。
- 非法 JSON / 无任何合法条目 → 报错，不写入。

---

## 3. 架构与改动清单

### 3.1 新增文件

| 文件 | 职责 |
|------|------|
| `src/utils/connectionTransfer.ts` | **纯逻辑模块（本功能核心）**：`serializeConnections` / `parseConnections` / `resolveImportItems`。不依赖 Pinia / Vue / UI，只做"数据 ↔ 文本"，由消费者调用 |
| `src/utils/clipboard.ts` | `copyToClipboard(text): Promise<boolean>` —— 优先 `navigator.clipboard.writeText`，失败回退临时 `textarea` + `execCommand('copy')` |
| `src/components/ConnectionTransferDialog.vue` | 共享弹窗，`mode: 'export' \| 'import'`<br/>• export：只读文本框（自动全选）+「复制到剪贴板」+「关闭」<br/>• import：可编辑文本框（占位提示"粘贴连接配置 JSON"）+「导入」+「关闭」 |

### 3.2 修改文件

| 文件 | 改动 |
|------|------|
| `src/stores/auth.ts` | 仅新增 **1 个编排方法** `importConnections(text)`：调模块解析 → 去重 → 逐条 `saveAccount` → 刷新。序列化/解析逻辑已移至 `connectionTransfer.ts` 模块 |
| `src/components/LoginDialog.vue` | • 每条 `account-item` 增加「复制」`text` 按钮<br/>• 页脚新增「复制全部」「粘贴导入」按钮<br/>• 挂载 `ConnectionTransferDialog`（export/import 两态）<br>• 引入图标：`DocumentCopy`、`Upload` |

### 3.3 不改动

- `utils/db.ts`：`saveAccount` / `getAllAccounts` 直接复用。
- `api/nameson.ts` / HTTP 层：导入只落库，不发起任何网络请求。
- 现有「导出清单」文件下载按钮：保留，作为"导出文件"通道，与"复制"并存。

---

## 4. 分层实现（伪代码）

> **关键架构决策**：纯逻辑（序列化 / 解析 / 去重判定）全部独立在 `connectionTransfer.ts` 模块中，**不依赖 Pinia / Vue / UI**；`auth` store 只做"编排"（调模块 + 落库），UI 组件只做"触发与展示"。三者解耦，模块可被任意消费者复用。

### 4.1 模块 `src/utils/connectionTransfer.ts`（纯逻辑，无副作用）

```typescript
import type { StoredAccount } from './db'

export interface ConnectionExport {
  name: string; serverUrl: string; user: string; password: string
}

// 序列化：连接列表 → 含明文密码的 JSON 文本（D2）
export function serializeConnections(list: StoredAccount[]): string

// 解析：文本 → 校验后的 ConnectionExport[]（兼容单条/数组）
export function parseConnections(text: string): { ok: boolean; items: ConnectionExport[]; error?: string }

// 去重判定：依现有连接名剔除重名，产出可 saveAccount 的列表（D4: 重名跳过）
export function resolveImportItems(
  items: ConnectionExport[],
  existingNames: string[],
): { toImport: StoredAccount[]; skipped: number }
```

### 4.2 store `src/stores/auth.ts`（thin orchestrator）

```typescript
import { parseConnections, resolveImportItems } from '@/utils/connectionTransfer'

async function importConnections(text: string): Promise<{ imported: number; skipped: number; error?: string }> {
  const parsed = parseConnections(text)          // 1. 解析（模块）
  if (!parsed.ok) return { imported: 0, skipped: 0, error: parsed.error }
  const latest = await getAllAccounts()
  const { toImport, skipped } = resolveImportItems(parsed.items, latest.map(a => a.name)) // 2. 去重（模块）
  for (const acc of toImport) await saveAccount(acc)   // 3. 落库（db）
  await loadAccounts()                                // 4. 刷新内存
  return { imported: toImport.length, skipped }
}
```

### 4.3 UI `LoginDialog.vue` / `ConnectionTransferDialog.vue`（消费者）

- `LoginDialog`：点「复制」→ `serializeConnections([account])` 交给弹窗；点「粘贴导入」→ 弹窗 `@import` 事件 → `auth.importConnections(text)` → `ElMessage` 反馈。
- `ConnectionTransferDialog`：导出模式自己完成「复制到剪贴板」（`copyToClipboard`）；导入模式仅把文本框内容 `emit('import')` 出去，不碰任何业务逻辑。

---

## 5. UI 交互细节

### 5.1 「已保存连接」页改动示意

```
现有页脚:
┌─────────────────────────────────────────┐
│ [新建连接]                    [导出清单]  │
└─────────────────────────────────────────┘

改动后页脚:
┌─────────────────────────────────────────────────────────┐
│ [新建连接] [复制全部] [粘贴导入]        [导出清单]        │
└─────────────────────────────────────────────────────────┘

每条连接项（account-item）操作区新增「复制」:
┌─────────────────────────────────────────────┐
│ 生产环境-主库                       [复制]  │
│ admin@http://...                 [重连][删除]│
└─────────────────────────────────────────────┘
```

### 5.2 ConnectionTransferDialog 行为

| 模式 | 文本框 | 主按钮 | 成功反馈 |
|------|--------|--------|---------|
| `export`（单条/全部） | `readonly`，打开自动全选 | 「复制到剪贴板」→ 调 `copyToClipboard`，成功 `ElMessage.success('已复制，可 Ctrl+V 分享')` | 显式提示 |
| `import` | 可编辑，placeholder="在此粘贴连接配置 JSON（支持单条或数组）" | 「导入」→ `parseConnections` → 错误则 `ElMessage.error` 并保留文本；成功调 `importConnections` 并 `ElMessage.success('已导入 N 条，跳过 M 条')` | 关闭弹窗 |

### 5.3 边界与异常处理

- 空粘贴 → `ElMessage.warning('请先粘贴连接配置')`。
- 非法 JSON / 无有效条目 → 报错，文本框保留可编辑，不关闭。
- 部分非法（数组里个别坏条目）→ 导入合法的，最后汇总提示「跳过 X 条无效 / 重名」。
- 重名跳过：因 `StoredAccount` 以 `name` 为 IndexedDB key，按 D4 选择**跳过**而非覆盖，避免误改现有连接。

---

## 6. 安全提示（重要）

- 导出文本含**明文密码**，会出现在：剪贴板、弹窗文本框、`copyToClipboard` 临时节点中。弹窗内加一行灰字提示：
  > ⚠ 配置含明文密码，请仅通过可信渠道分享，复制后及时清空剪贴板。
- 导入落库后密码仍以明文存于 IndexedDB（与现有 `doLogin` 行为一致，无新增风险）。
- 导入**绝不自动连接**，接收方需主动点「重连」并重新鉴权，避免凭据被静默使用。

---

## 7. 影响评估

| 维度 | 评估 |
|------|------|
| 改动量 | 小：2 个文件改动 + 2 个新文件（1 工具 + 1 弹窗） |
| 风险等级 | 低 — 不触碰任何现有 API / HTTP 层 / 数据模型 |
| 回归面 | 仅「连接管理」弹窗；现有登录、切换、联动功能不受影响 |
| 新增依赖 | 无（图标用 Element Plus 自带；clipboard 用浏览器原生 API） |

---

## 8. 待确认 / 备注

- [ ] 弹窗内安全提示文案是否接受（§6）。
- [ ] 「复制全部」是否与「导出清单」在页脚并排（文档默认并排）。
- [ ] 是否需要给单条「复制」也走弹窗（默认走弹窗，便于 HTTP 下手动复制，与 D3 一致）。

> **下一步**：用户确认本方案后，进入实现（预计 2 个新文件 + 2 处改动，可一次完成）。
